<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="rectangle">
    <gradient
            android:startColor="@color/colorButtonHighlight"
            android:centerColor="@color/colorButton"
            android:endColor="@color/colorButton"
            android:type="linear"
            android:angle="135"/>
</shape>